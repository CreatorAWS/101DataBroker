function checksmsblast()
	{
		if ($('.admintable-control-checkbox:checked').length>0)
			$('.smsblast').show();
		else
			$('.smsblast').hide();
	}

function checkmovecontacts()
	{
		if ($('.admintable-control-checkbox:checked').length>0)
			$('.smsblast').show();
		else
			$('.smsblast').hide();
}